package com.hasbi.commands.orderCommands;

import com.hasbi.commands.BaseCommand;

public class DeleteOrderCommand extends BaseCommand<String> {
    public DeleteOrderCommand(String commandId) {
        super(commandId);
    }
}
