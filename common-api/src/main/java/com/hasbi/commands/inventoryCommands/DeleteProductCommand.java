package com.hasbi.commands.inventoryCommands;

import com.hasbi.commands.BaseCommand;

public class DeleteProductCommand extends BaseCommand<String> {
    public DeleteProductCommand(String commandId) {
        super(commandId);
    }
}
